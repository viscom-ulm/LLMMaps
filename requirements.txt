#Packages for the visualization
anytree >= 2.8.0
svgwrite >= 1.4.3
svgpathtools >= 1.6.0
tensorflow >= 2.3.0
SecretColors >= 1.2.6
shapely >= 2.0.1    
#For data processing
nltk >= 3.7
numpy >= 1.23.4 
openai >= 0.27.0    
tiktoken >= 0.2.0
datasets >= 2.10.1
transformers >= 4.28.0.dev0 #Fix of the standard package to make LLaMA work at pip install git+https://github.com/mbehm/transformers
